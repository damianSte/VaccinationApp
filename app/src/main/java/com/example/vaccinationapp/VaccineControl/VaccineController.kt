package com.example.vaccinationapp.VaccineControl

import android.os.Bundle
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity
import androidx.core.view.ViewCompat
import androidx.core.view.WindowInsetsCompat
import com.example.vaccinationapp.R
import java.sql.Date

class VaccineController : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }

    private fun addVaccine(name: String, dateAdministrated: Date, nextDoseDue: Date){
        //TODO (Crate procedure to add vaccine implement here)

    }
}